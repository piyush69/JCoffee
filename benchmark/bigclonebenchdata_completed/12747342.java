import java.io.*;
import java.lang.*;
import java.util.*;
import java.math.*;
import java.net.*;
import java.applet.*;
import java.security.*;



class c12747342 {

    public void bubbleSort(final int[] s)  throws Throwable {
        MyHelperClass source = new MyHelperClass();
        source = s;
        MyHelperClass source = new MyHelperClass();
        if (source.length < 2) return;
        boolean go = true;
        while (go) {
            go = false;
            MyHelperClass source = new MyHelperClass();
            for (int i = 0; i < source.length - 1; i++) {
                MyHelperClass source = new MyHelperClass();
                int temp = source[i];
                MyHelperClass source = new MyHelperClass();
                if (temp > source[i + 1]) {
                    MyHelperClass source = new MyHelperClass();
                    source[i] = source[i + 1];
                    MyHelperClass source = new MyHelperClass();
                    source[i + 1] = temp;
                    go = true;
                }
            }
        }
    }

}

// Code below this line has been added to remove errors
class MyHelperClass {

}
