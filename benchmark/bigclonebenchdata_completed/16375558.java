import java.io.*;
import java.lang.*;
import java.util.*;
import java.math.*;
import java.net.*;
import java.applet.*;
import java.security.*;



class c16375558 {

    private void sort()  throws Throwable {
        MyHelperClass density = new MyHelperClass();
        for (int i = 0; i < density.length; i++) {
            MyHelperClass density = new MyHelperClass();
            for (int j = density.length - 2; j >= i; j--) {
                MyHelperClass density = new MyHelperClass();
                if (density[j] > density[j + 1]) {
                    MyHelperClass nonEmptyNodesArray = new MyHelperClass();
                    KDNode n = nonEmptyNodesArray[j];
                    MyHelperClass nonEmptyNodesArray = new MyHelperClass();
                    nonEmptyNodesArray[j] = nonEmptyNodesArray[j + 1];
                    MyHelperClass nonEmptyNodesArray = new MyHelperClass();
                    nonEmptyNodesArray[j + 1] = n;
                    MyHelperClass density = new MyHelperClass();
                    double d = density[j];
                    MyHelperClass density = new MyHelperClass();
                    density[j] = density[j + 1];
                    MyHelperClass density = new MyHelperClass();
                    density[j + 1] = d;
                }
            }
        }
    }

}

// Code below this line has been added to remove errors
class MyHelperClass {

}

class KDNode {

}
